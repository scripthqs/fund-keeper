# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 数据验证（FastAPI 依赖，但代码中直接 import，建议显式声明）
pydantic>=2.0.0

# 环境变量管理
python-dotenv>=1.0.0

# LLM 依赖
openai>=1.12.0

# 图片处理（截图压缩）
Pillow>=10.0.0

# 中国节假日判断
chinese-calendar>=1.10.0

# HTTP 异步客户端（天天基金 API 调用）
httpx>=0.27.0
